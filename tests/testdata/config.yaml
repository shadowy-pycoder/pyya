app:
  name: myapp_changed

database:
  user: myuser_changed
  password: mypass_changed
  name: mydb_changed

logging:
  level: debug
  rotate:
    enabled: 0

cache:
  servers:
    - host: cache1.local
      port: 6379
  ttlSeconds: 500

admins:
  - name: Alice
    email: alice@example.com

extra_section:
  extra: true
